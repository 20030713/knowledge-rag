# 项目架构与流程图

本文档用于项目展示、答辩和面试讲解，重点说明系统边界、核心链路和模块协作关系。

## 1. 系统定位

企业知识库 RAG 问答系统不是普通聊天机器人，而是一个围绕“私有知识入库、检索、问答、引用、反馈”的完整后端项目。

核心目标：

- 用户可以维护自己的知识库
- 系统可以解析上传文档并生成文本切片
- 问答时只在当前知识库范围内召回片段
- 回答返回引用来源和检索分数
- 系统记录问答历史、任务状态和用户反馈
- Redis 参与限流、缓存和分布式锁

## 2. 总体架构

```mermaid
flowchart TB
    User["用户 / 浏览器"] --> Frontend["React + Vite 前端"]
    Frontend --> API["Spring Boot REST API"]

    API --> Auth["认证鉴权模块 JWT"]
    API --> KB["知识库模块"]
    API --> Doc["文档模块"]
    API --> Rag["RAG 问答模块"]
    API --> Dashboard["仪表盘 / 任务中心"]

    Doc --> Async["Spring Async 文档解析任务"]
    Async --> Reader["PDFBox / POI / 文本读取器"]
    Reader --> Chunker["TextChunker 文本切分"]
    Chunker --> MySQLChunk["MySQL document_chunk"]

    Rag --> Cache["Redis 问答缓存"]
    Rag --> Vector["本地 Embedding + 向量检索"]
    Vector --> MySQLChunk
    Rag --> QaLog["MySQL qa_record"]

    API --> RedisLimit["Redis 限流"]
    Doc --> RedisLock["Redis 分布式锁"]
    KB --> Cleanup["级联清理"]

    KB --> MySQL["MySQL 业务数据"]
    Doc --> MySQL
    Dashboard --> MySQL
    Cleanup --> RedisCache["Redis 缓存清理"]
    Cleanup --> FileStore["本地上传文件"]
```

## 3. 核心业务边界

| 模块 | 职责 | 数据边界 |
|---|---|---|
| 用户认证 | 注册、登录、JWT 鉴权 | `user` |
| 知识库 | 创建、编辑、删除知识库 | `knowledge_base` |
| 文档 | 上传、解析、删除、状态流转 | `document` |
| 切片 | 保存解析后的 chunk | `document_chunk` |
| RAG 问答 | 检索、回答、引用、缓存 | `qa_record`, `document_chunk`, Redis |
| 任务中心 | 查询解析任务、失败重试 | `document.status` |
| 反馈闭环 | 保存回答质量反馈 | `qa_record.feedback_*` |
| Redis 工程增强 | 限流、缓存、分布式锁 | Redis |

## 4. 文档入库流程

```mermaid
sequenceDiagram
    participant U as 用户
    participant F as 前端
    participant D as DocumentController
    participant S as DocumentService
    participant R as Redis Lock
    participant A as Async Task
    participant P as 文档解析器
    participant C as TextChunker
    participant DB as MySQL

    U->>F: 上传 PDF / Word / Markdown / TXT
    F->>D: POST /api/doc/upload
    D->>S: 保存文档元数据
    S->>DB: document.status = UPLOADED
    S-->>F: 返回文档记录

    U->>F: 点击解析
    F->>D: POST /api/doc/{id}/parse
    S->>R: 获取 lock:doc:parse:{id}
    S->>DB: document.status = PARSING
    S->>A: 提交异步任务
    D-->>F: 立即返回 PARSING

    A->>P: 提取文档文本
    P->>C: 清洗并切分 chunk
    C->>DB: 写入 document_chunk
    A->>DB: document.status = COMPLETED
    A->>R: Lua 校验并释放锁
    F->>D: 轮询 GET /api/doc/{id}/status
    F->>D: 加载 chunk 预览
```

## 5. RAG 问答流程

```mermaid
sequenceDiagram
    participant U as 用户
    participant F as 前端
    participant R as RagController
    participant L as Redis 限流
    participant C as Redis 问答缓存
    participant V as VectorSearchService
    participant DB as MySQL

    U->>F: 输入问题
    F->>R: POST /api/rag/ask
    R->>L: 校验用户请求频率
    R->>C: 查询 user + kb + questionHash 缓存
    alt 缓存命中
        C-->>R: 返回 RagAskResponse
        R-->>F: 返回答案和引用
    else 缓存未命中
        R->>V: 本地 Embedding + 混合检索
        V->>DB: 查询当前知识库 chunk
        V-->>R: TopK 引用片段
        R->>DB: 保存 qa_record
        R->>C: 写入短期问答缓存
        R-->>F: 返回答案、引用、检索分数
    end
```

## 6. Redis 使用图

```mermaid
flowchart LR
    Redis["Redis"]
    Rate["接口限流<br/>rate_limit:*"]
    Cache["RAG 问答缓存<br/>rag:answer:user:*"]
    Lock["文档解析锁<br/>lock:doc:parse:*"]

    Redis --> Rate
    Redis --> Cache
    Redis --> Lock

    Rate --> Login["登录接口按 IP 限流"]
    Rate --> Ask["问答接口按用户限流"]
    Cache --> Hit["重复问题直接返回缓存"]
    Cache --> Evict["文档变化后 SCAN 清理"]
    Lock --> Parse["同一文档只允许一个解析任务"]
```

## 7. 数据一致性设计

```mermaid
flowchart TB
    DeleteKB["删除知识库"] --> Tx["数据库事务"]
    Tx --> DelChunk["删除 document_chunk"]
    Tx --> DelQa["删除 qa_record"]
    Tx --> DelDoc["删除 document"]
    Tx --> DelKb["删除 knowledge_base"]
    Tx --> Commit["事务提交"]
    Commit --> AfterCommit["afterCommit 回调"]
    AfterCommit --> DelRedis["清理 Redis 问答缓存"]
    AfterCommit --> DelFile["删除本地上传文件"]
```

关键点：

- 数据库内的数据用事务保证一致性
- Redis 和文件系统属于外部资源，放到事务提交后处理
- 外部资源清理失败只记录日志，后续可通过定时任务补偿

## 8. 当前技术亮点

- JWT 登录鉴权和用户级数据隔离
- 多格式文档解析：PDFBox + Apache POI + TXT/Markdown
- Spring Async 异步解析任务
- 文档状态机和失败重试保护
- Redis 固定窗口限流
- Redis RAG 问答缓存
- Redis `SET NX EX` + Lua 分布式锁
- 本地 Embedding + 向量检索抽象
- 向量分数 + 关键词分数混合排序
- 引用来源和检索分数透明展示
- 任务中心和失败任务重试
- 问答反馈闭环
- 知识库删除级联清理和事务 afterCommit
